import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Colors.black;
  static const Color loginBackground = Color(0xFF3A92DA);
  static const Color errorColor = Colors.redAccent;
  static const Color primaryTransColor = Color.fromARGB(169, 0, 0, 0);
  static const Color bottomSheetColor = Color.fromARGB(235, 0, 0, 0);
  static const Color primaryTextColor = Colors.white;
}
